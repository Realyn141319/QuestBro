import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./index.css";

// Disable StrictMode for react-beautiful-dnd compatibility
ReactDOM.createRoot(document.getElementById("root")).render(<App />);
